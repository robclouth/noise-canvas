/** The palette holding the one brush a fresh install starts with. */
export const DEFAULT_PALETTE_ID = "palette-default";

/** Name an open palette takes until it is saved to a file. */
export const UNTITLED_PALETTE_NAME = "Untitled";
